# Floor 3 Tree Rework — Implementation Plan

> **Status:** CODE COMPLETE — awaiting Phase 9 human playtest
> **Owner file:** `plans/floor3-tree-rework.md` (this file)
> **Scope isolation:** Other agents are working on unrelated plans in parallel. Touch **only** the files listed in §4 "File Inventory". If you believe a file outside that list must change, write the reason in the Journal (§9) first, then change it.

---

## 1. Goal

Replace the six hand-drawn tree PNGs on the Level 3 overworld with a **procedurally generated, offline-baked tree sprite family** that:

1. **Looks like one forest** — many distinct trees, one shared palette/canopy engine, so variety never reads as "assets from different games".
2. **Is taller than one tile** — the base occupies one tile; the canopy climbs three tiles above it.
3. **Sorts correctly against entities** — the player walks *behind* a tree whose base is south of their feet, and *in front of* a tree whose base is north of their feet. (Today trees always draw behind everything — see §3.)
4. **Is destructible** — significantly tougher than a crate/barrel.
5. **Burns.** Every magic missile that hits a tree has a **10% chance to ignite it**. A burning tree burns down to a **black charred husk** that is then trivially destroyed.
6. **Hurts the player on contact while burning** — standing adjacent deals damage immediately, and applies the existing `burn` status once the player has been in contact for **1 second**.
7. **Reacts to explosives** — dynamite **always destroys** every tree in its blast radius and **ignites** the trees in a ring just outside it.

---

## 2. Non-Goals (do NOT build these)

- **Ambient fire spread between trees.** Only explosives ignite neighbours. A burning tree does *not* set its neighbours alight on its own. Leave a single well-named hook (`TreeSystem.igniteRadius`) so this can be added later, but do not call it from the burn tick.
- **New audio files.** Reuse existing `SoundId`s only. Do not add `.mp3` assets.
- **Burning mobs.** Trees damage *players* on contact. Mob-vs-fire is out of scope.
- **Trees on any floor other than 3.** `TREE` (tile type 13) is only generated by `OverworldGenerator`. Do not add trees to dungeon floors.
- **Persisting tree damage across save/reload.** Tree state is per-session.
- **Reworking `grassy_weed` / `dirt_patch` / any other nature asset.**
- **Animated idle sway.** `idle` is a single static frame. (Sway would force every tree tile out of the static path every frame; not worth it at forest density.)

---

## 3. Current State — Verified Facts

Everything below was verified against the codebase on 2026-08-01. Trust it, but re-read the cited line before editing (line numbers drift).

### 3.1 Art today
- `src/images/environment/nature/Tree1.png` … `Tree6.png`, hand-drawn.
- `src/images/environment/nature/tree.png` is registered as manifest key `tree` but **referenced by no code**. Dead asset.
- Manifest: `src/images/environment/nature/manifest.json`. Each `tree_1`…`tree_6` is `tileScale: 32`, `tileX: 0`, `frameWidth: 32`, `frameHeight` 49–62, `tileY` 17–30, one `idle` state, 1 frame.
- `frameHeight - tileY === 32` for every variant, i.e. each tree overhangs upward by `tileY` px (~0.6 tile) and never sideways.
- **No tree manifest entry declares `tileTypeId`.** This matters — see §3.4.

### 3.2 Variant selection
`src/map/tiles/decorationTiles.ts:622-647` — `case TREE:` picks one of six sprite keys with a position hash:
```ts
switch (((Math.imul(tx, 2654435761) ^ Math.imul(ty, 2246822519)) >>> 0) % 6) { … }
```
Stable across frames and re-bakes, no per-tile storage.

### 3.3 The two decoration registries (KNOWN TRAP)
A Y-sorted tile must be in **both**:
1. `DECORATION_TYPES` — `src/map/TileRenderer.ts:71-94` (makes the chunk bake draw ground only). `TREE` is at :72.
2. `DECORATION_OVERLAY_TYPES` — `src/map/GameMap.ts:166-188` (the set `buildDecorationTile` actually consults). `TREE` is at :167.

`TREE` is already in both. Also relevant: `CACHEABLE_OVERLAY_TYPES` (`TileRenderer.ts:102-115`) deliberately **excludes** `TREE` — a PNG-sprite tile is already one `drawImage`, so no offscreen cache. Keep it excluded; a burning tree animates and must not be cached.

The baseOnly branch that paints grass under a tree is `decorationTiles.ts:594-599`.

### 3.4 Sorting — the core defect
`src/systems/RenderPipeline.ts:56-57, 238-256`:
```ts
const TREE_SORT_DEPTH_OFFSET = 100000;
…
e.sortY = isTree ? ty - TREE_SORT_DEPTH_OFFSET : ty * TILE_SIZE + sortYAnchorPx;
```
Trees get a sort key in **tile** units, offset by −100000, so **every tree sorts before every entity**. The player therefore always draws on top of a tree, never behind it. `isTree` (`GameMap.ts:158, 2114`) is the only type-specific flag on `DecorationTile` and exists solely to feed this branch.

Every other decoration uses the correct key: `ty * TILE_SIZE + sortYAnchorPx`, where `sortYAnchorPx = (frameHeight - tileY) * (TILE_SIZE / tileScale)` (computed `SpriteLoader.ts:246-255`, read via `getSortYAnchorPx(tileTypeId)`).

Because no tree manifest entry declares `tileTypeId`, `getSortYAnchorPx(TREE)` returns `undefined` and `GameMap.ts:2115` falls back to `ts`. Likewise `decorationTileExtentsPx` (`TileRenderer.ts:418-442`) misses and falls back to `unregisteredDecorationExtents(ts)` = 1 tile in every direction. **A 3-tile-tall tree will be clipped/culled unless a manifest entry declares `tileTypeId: 13`.**

`BuildingInteriorScene.ts:1493` already uses the generic formula and has no `isTree` case — no change needed there.

### 3.5 Walkability
`src/map/walkability.ts:49-55` — `TREE ∈ NON_WALKABLE_TILE_TYPES`; `:246-252` bakes it into `WALKABLE_BY_TILE_TYPE`. Whole-tile granularity, no sub-tile hitbox. This is already the desired behaviour for the base: one blocked tile.

### 3.6 Placement
`src/map/OverworldGenerator.ts:145-152` (constants) and `:912-935` (`paintForests`), called at `:234`. 30 blobs, radius 8–21, unseeded `Math.random()`, `grid.set(tx, ty, TREE)`. Ground material for a tree tile is forced to `'grass'` at `src/map/town/groundMaterials.ts:166-173`.

### 3.7 Destructible props — the pattern to mirror
`src/systems/DestructiblePropSystem.ts`:
- Props are **tiles, not entities**. HP lives in `private readonly health = new Map<string, PropHealth>()`, keyed by `tileKey(tx, ty)` (`src/systems/tileKey.ts`), **created lazily on first hit**.
- Persistent visual state lives on the tile: `tile.damageStage = PROP_DAMAGE_STAGE_CRACKED` (`src/map/tileTypes.ts:387-400`, plus `propSpriteState()`).
- Public API: `tryMeleeHit(attacker, range, damage)`, `tryAreaHit(attacker, radius, damage)`, `tryProjectileHit(x, y, radius, damage, owner)`, `destroyInRadius(x, y, radius, owner)`, `drainSmashes()`.
- All funnel into `damagePropsInRange(...)` (:291), which brute-scans a square tile window, tests Euclidean distance, applies an optional facing cone beyond `MELEE_POINT_BLANK_RANGE`, and LOS-checks with the prop's own tile exempted.
- Destruction (`breakProp`, :379): `tile.type = tile.groundType ?? inferFloorType(...)`, `delete tile.damageStage`, `gameMap.markTileDirty(...)`, then shatter burst + splinters + wreckage decal + `loot.addLoot(...)`.
- HP scale for calibration: torch 4, barrel_side 5, barrel 6, crate 6, bookshelf 8, brazier 9.
- **Instantiated only on non-overworld levels** — `DungeonScene.ts:889-894` passes `null` when `levelDef.isOverworld`. Trees are overworld-only, which is exactly why they need their own system.

### 3.8 Combat / missile / dynamite seams
- `src/systems/CombatSystem.ts` — `CombatContext` carries `destructibles?: DestructiblePropSystem` (:60). Missile resolution `resolvePlayerAttacks` (:67); **props are checked before mobs** at :228-237 via `tryProjectileHit`, then mobs at :239-291. Melee: human :111-114, cat :147-149. Smush: :199-204.
- Missiles are plain records — `interface Missile` in `src/sprites/catSprite.ts:9-23`, owned by `CatPlayer` (`getMissiles()`, `updateMissiles()`). Wall impact sets `state = 'exploding'` in `CatPlayer.updateMissiles`.
- `src/systems/DynamiteSystem.ts:155-186` `triggerExplosion(...)`: `DYN_RADIUS = TILE_SIZE * 3`, `DYN_DAMAGE = 8`, damages mobs/human/cat by flat amount (no falloff, not LOS-gated), then `this.destructibles?.destroyInRadius(cx, cy, DYN_RADIUS, human)`. Constructor already takes `destructibles: DestructiblePropSystem | null = null`.

### 3.9 Status effects & contact damage
- `src/core/StatusEffect.ts` — `makeBurn()` (:20) already exists: `type: 'burn'`, 480 ticks (8 s). **Reuse it. Do not invent a new status.**
- Ticked in `Player.tickStatusEffects()` (`src/Player.ts:765`, DoT branch :775): 1 dmg every `BURN_TICK_INTERVAL = 60` ticks. Visual: `renderStatusEffects` (:1041-1080) draws the pulsing flame.
- Death attribution already handles it: `src/systems/DeathCauseSystem.ts:36` maps `'burn'` → `'burnedStatus'`.
- **Contact-field precedent to copy:** `BossRoomSystem.tickAcidPuddles` (`src/systems/BossRoomSystem.ts:757`, called from `update` at :592). Per-player accumulator (`humanAcidTick` / `catAcidTick`) that increments only while inside the field and **resets to 0 the moment the player leaves**, gating `takeDamage` on `counter % INTERVAL === 0`. Damage source uses `undodgeable: true` (`src/Player.ts:66-83`) — "dodging a floor you are standing on makes no sense".
- Precedent for a 10%-style ignite roll: `src/creatures/Llama.ts:35,111` — `const BURN_CHANCE = 0.15; … if (connected && Math.random() < BURN_CHANCE) t.applyStatus(makeBurn());`

### 3.10 Generated-art pipeline
- All PNG generation uses **node-canvas** (`canvas` devDependency) run under **`tsx`**: `createCanvas` → `ctx.draw*` → `canvas.toBuffer('image/png')` → `writeFileSync`. Committed to `src/images/**`. **No build step regenerates art.**
- Closest model to copy: `scripts/generate-destructible-props-sprite.ts` (2316 lines) — seeded `makeRng` (mulberry32, :129, "so re-running the script produces byte-identical sheets and diffs stay meaningful"), named colour ramps, shared primitives (`contactShadow`, `seam`, `crack`, `chip`, `shard`, `puff`, `speckle`), a `FrameGeometry` describing where the logical tile sits inside an oversized frame, and a `renderSheet(kind)` that lays out rows=states / cols=frames.
- Sheet packing + manifest emission helper: `scripts/townSheets.ts` — `SheetSpec`, `renderSheet()` (clips each frame to its own cell), `manifestEntry()`, `writeSheets(dir, tileScale, sheets)`. **`writeSheets` REPLACES the whole directory's `manifest.json`** — which is why trees get their own directory (§5.1).
- Canonical seeded RNG for reuse: `src/sprites/person/rng.ts` — `mulberry32`, `range`, `rangeInt`, `pick`, `chance`, `centered`, `subSeed(seed, salt)`.
- If a painter blits an existing sprite, the generator must `await loadGameSpritesInNode()` from `scripts/nodeCanvasGlobals.ts` first, or `drawSpriteKey` silently no-ops and blanks bake in permanently. (Tree painters should be self-contained, so this should not be needed — but if you add one, remember it.)
- **A new image directory requires a new `import … from '…/manifest.json'` line in `src/core/SpriteLoader.ts:1-15` and a spread into `environmentManifest` (:18-32).** Miss this and nothing loads.
- `scripts/shipped-assets.js` only ships files named by a manifest `path` or a `src/**/*.ts` path literal — an unmanifested PNG is silently excluded from the service worker / ZIP / Pages build.
- Add new scripts to `tsconfig.scripts.json`'s `include` so `npm run typecheck` covers them.
- Dev review routes are registered in `devBootScene` (`src/game.ts:38-90`, localhost-only), e.g. `?tiles` → `TilePreviewScene`. Headless contact-sheet harnesses: `scripts/render-clowns.ts` (reads geometry from the manifest, renders a strip at in-game tile size), `scripts/render-town.ts` (real town through the real render path — ideal for judging a forest in situ).

---

## 4. File Inventory

Files this plan is allowed to create or modify. Anything else needs a Journal note first.

**New**
- `scripts/generate-tree-sprites.ts` — the generator
- `scripts/treeArt.ts` — the shared painter library (species, canopy engine, palettes, burn/char stages)
- `scripts/render-trees.ts` — headless contact-sheet harness
- `src/images/environment/trees/*.png` + `src/images/environment/trees/manifest.json` — generated output (committed)
- `src/systems/TreeSystem.ts` — HP, burn lifecycle, contact damage, ignition, rendering of fire/embers

**Modified**
- `package.json` — add `gen:trees` script
- `tsconfig.scripts.json` — `include` the three new scripts + `scripts/treeArt.ts`
- `src/core/SpriteLoader.ts` — import + spread the new manifest
- `src/map/tileTypes.ts` — tree stage constants + `treeStage`/burn fields on `TileContent`, `treeSpriteState()` helper
- `src/map/tiles/decorationTiles.ts` — `case TREE:` picks species/variant/state/frame
- `src/systems/RenderPipeline.ts` — delete the `isTree` sort branch and `TREE_SORT_DEPTH_OFFSET`
- `src/map/GameMap.ts` — delete `isTree` from `DecorationTile` and `buildDecorationTile`
- `src/systems/CombatSystem.ts` — `trees?: TreeSystem` on `CombatContext`; melee/area/missile hooks; 10% missile ignite
- `src/systems/DynamiteSystem.ts` — accept `TreeSystem | null`; destroy + ignite ring in `triggerExplosion`
- `src/scenes/DungeonScene.ts` — construct `TreeSystem` on overworld levels, wire update/render/audio drain, pass to `CombatSystem` + `DynamiteSystem`
- `src/map/walkability.ts` — only if a charred husk should become walkable (decide in Phase 5; default: it stays blocking)
- `src/scenes/TownMapScene.ts` — minimap colour for charred trees (`:92` has `[TREE, '#1f3d2b']`)

**Deleted (Phase 8 only, after the new art ships)**
- `src/images/environment/nature/Tree1.png` … `Tree6.png`, `tree.png`
- the `tree`, `tree_1`…`tree_6` entries in `src/images/environment/nature/manifest.json`

---

## 5. Design Decisions (settled — do not re-litigate)

### 5.1 New directory, not `nature/`
Trees go in **`src/images/environment/trees/`** with their own `manifest.json`. Reason: `writeSheets` replaces a directory's entire manifest, and `nature/manifest.json` also holds `grassy_weed` and `dirt_patch`, which would be destroyed. This also matches the `townscape/` vs `towns/<town>/` folder discipline already in the repo.

### 5.2 One geometry envelope for every tree
Every tree sheet shares **identical frame geometry**:

| field | value | meaning |
|---|---|---|
| `tileScale` | `32` | matches `TILE_SIZE`; 1:1 blit, no rescale |
| `frameWidth` | `96` | 3 tiles — 1 tile of canopy overhang each side |
| `frameHeight` | `128` | 4 tiles — base tile + 3 tiles of trunk/canopy above |
| `tileX` | `32` | anchor tile is the middle column |
| `tileY` | `96` | anchor tile's top edge; `frameHeight - tileY === 32` ✅ |

**Why one envelope:** `tileTypeId` is a 1:1 map to geometry in `SpriteLoader` (`_tileSortYAnchorPx`, `_tileSpriteOverheadPx`, `_tileSpriteExtentsPx` are all keyed by tile-type id, last write wins). Shorter species simply leave transparent pixels — transparent pixels are free, a clipped baked frame is permanent.

**`tileScale` stays 32, not 64.** The town-art lesson applies: painters that mix fixed-pixel constants get silently thinned when baked at 64 and halved at draw time.

### 5.3 Exactly one key declares `tileTypeId: 13`
Add `"tileTypeId": 13` to **one** canonical tree entry (`tree_oak_a`). This is what makes `getSortYAnchorPx(TREE)` return 32 and `getSpriteExtentsPxForTileType(TREE)` return `{left:32, right:32, up:96, down:0}` instead of the 1-tile fallback. Declare **no** `blockedRegions` — walkability is already handled by `NON_WALKABLE_TILE_TYPES`, and a `blockedRegions` entry would start blocking canopy tiles.

`writeSheets` must be able to emit `tileTypeId`; if it cannot, extend `SheetSpec`/`manifestEntry` in `scripts/townSheets.ts` to carry an optional `tileTypeId` rather than hand-editing the generated JSON.

> ⚠️ **Perf checkpoint (Phase 3):** `up: 96` equals `OVERSIZED_DECORATION_EXTENT_TILES * TILE_SIZE` exactly, and the test is `> limit`, so trees stay in the *modest* row buckets and widen `_modestDecorationExtents.up` to 96 px for **every** decoration's row scan (`GameMap.ts:190-199, 2008-2054`). Measure the frame time in a dense forest before and after. If it regresses, do **not** shrink the tree — bump the up-extent to 97 so trees fall into `_oversizedDecorations` instead, and re-measure.

### 5.4 Species and variants — "varied, but one forest"
**3 species × 4 variants = 12 sprite keys.**

| species | key prefix | silhouette |
|---|---|---|
| oak (broadleaf) | `tree_oak_a`…`_d` | wide, lobed, heavy crown, thick forking trunk |
| birch (slender) | `tree_birch_a`…`_d` | narrow, tall, pale bark, airy crown |
| pine (conifer) | `tree_pine_a`…`_d` | conical, layered boughs, straight bole |

Cohesion rules — **all twelve share**:
- one `FOLIAGE_RAMP` and one `BARK_RAMP` per species, all three species sampled from a single master palette so hues sit within a narrow band;
- one canopy engine (same clump/leaf-mass primitive, same light direction — **upper-left**, matching every other prop in the repo);
- one `contactShadow` treatment at the base;
- one silhouette budget (crown never exceeds `frameWidth`, base always centred on the anchor tile).

Variation comes from seeded parameters only: trunk lean, crown radius, clump count, branch fork angles, foliage-value jitter. Each variant uses a fixed literal seed (`makeRng(TREE_SEEDS.oak_a)` etc.) so re-runs are byte-identical.

### 5.5 Spatially coherent species placement
Species must be coherent so a forest reads as one wood, while individual trees still differ:
- **Species** from a low-frequency hash of `Math.floor(tx / GROVE_SIZE_TILES)`, `Math.floor(ty / GROVE_SIZE_TILES)` with `GROVE_SIZE_TILES = 24`.
- **Variant** from the existing per-tile hash (`Math.imul(tx, 2654435761) ^ Math.imul(ty, 2246822519)`) mod 4.

No per-tile storage; stable across frames and re-bakes, exactly like today.

### 5.6 Sheet rows (states)
Six rows per key, max 6 frames wide:

| row | state | frames | content |
|---|---|---|---|
| 0 | `idle` | 1 | healthy tree |
| 1 | `damaged` | 1 | bark gouged, boughs snapped, thinned crown |
| 2 | `burning` | 6 | flames climbing the crown, crown still green underneath |
| 3 | `burning_late` | 6 | crown mostly consumed, trunk alight, embers |
| 4 | `charred` | 1 | black skeletal husk, no leaves, no flame |
| 5 | `felling` | 6 | collapse — trunk topples, debris, dust/ash puff |

Plus a **shared** `remains` decal. Do **not** give each tree its own remains row; emit one `tree_remains.png` (single frame, boxed geometry like the props' remains) reused by all twelve — a stump and scattered ash reads the same regardless of species, and twelve near-identical rows is waste.

> `felling` and `remains` mirror the props' `shatter` / `remains` contract so `DestructiblePropSystem`'s animation timing constants (`SHATTER_FRAME_COUNT = 6`, `SHATTER_FRAMES_PER_STEP = 5`) can be mirrored rather than reinvented.

### 5.7 Burn lifecycle
```
idle ──(missile 10% | explosion ring)──▶ burning ──▶ burning_late ──▶ charred ──(any hit)──▶ destroyed
  │                                                                                             ▲
  └────────────────────────(enough melee/missile damage)────────────────────────────────────────┘
```
- Burning **never destroys the tree by itself** — it always terminates at `charred`. The user asked for "a black, charred object that could be easily destroyed".
- A charred husk is permanent until something breaks it.
- Fire cannot be extinguished.
- Igniting an already-burning or charred tree is a no-op.

### 5.8 Tuning constants (starting values — tune in Phase 7)
Put these in `TreeSystem.ts` as named constants. No bare literals.

| constant | value | rationale |
|---|---|---|
| `TREE_HP` | `28` | ~4.5× a crate (6) — "significantly harder" |
| `TREE_DAMAGED_HP_FRACTION` | `0.5` | switch to `damaged` art below half HP |
| `CHARRED_TREE_HP` | `3` | "easily destroyed" — under a crate |
| `BURNING_FRAMES` | `420` (7 s) | time in `burning` before `burning_late` |
| `BURNING_LATE_FRAMES` | `300` (5 s) | time in `burning_late` before `charred` |
| `MISSILE_IGNITE_CHANCE` | `0.10` | per the request |
| `EXPLOSION_IGNITE_RING_TILES` | `2` | ring outside `DYN_RADIUS` that catches fire |
| `TREE_CONTACT_RADIUS` | `TILE_SIZE * 1.1` | centre-to-centre; "standing next to it" |
| `TREE_CONTACT_DAMAGE_INTERVAL` | `30` (0.5 s) | contact damage cadence |
| `TREE_CONTACT_DAMAGE` | `2` | per tick |
| `TREE_CONTACT_BURN_DELAY` | `60` (1 s) | contact ticks before `makeBurn()` — per the request |
| `TREE_FLAME_FPS` | `10` | burn animation rate |
| `TREE_COIN_SPREAD` | `2` | drop scale (floor 3 → 3–5 coins) |

**Contact-damage semantics** (read carefully — this is the exact request):
> "Touching the tree as it is burning should hurt the player. Standing next to it so the player is 'touching' should cause them to take damage and then get the burned status after 1 second has passed."

So: damage starts on the **first** contact tick, and `makeBurn()` is applied once the accumulator reaches `TREE_CONTACT_BURN_DELAY`. The accumulator resets to 0 the moment the player breaks contact (acid-puddle pattern). Damage source: `{ kind: 'environmental' }` — or a `'mob'`-shaped source is *wrong* here since no mob is involved. Use `{ kind: 'environmental' }` and confirm `Player.takeDamage` treats it as undodgeable; if it does not, that is a real gap — note it in the Journal and add the handling.

### 5.9 Where tree state lives
Mirror the props exactly:
- **Volatile HP + burn timers:** `TreeSystem`'s `Map<string, TreeState>` keyed by `tileKey(tx, ty)`, created **lazily on first hit or ignition**. A 280×280 map with 30 forest blobs must cost zero until something is touched.
- **Renderer-visible stage:** on the tile itself (`TileContent`), because `drawDecorationTile` is a pure tile renderer with no access to systems — exactly how `TORCH`/`BRAZIER` read `structure[ty][tx].damageStage` at `decorationTiles.ts:663-698`. Add:
  - `treeStage?: number` — `TREE_STAGE_HEALTHY | DAMAGED | BURNING | BURNING_LATE | CHARRED`
  - `treeBurnFrame?: number` — animation cursor, advanced by `TreeSystem.update()`

  and a `treeSpriteState(stage)` helper next to `propSpriteState` in `src/map/tileTypes.ts`.
- **Destruction** follows `breakProp`: `tile.type = tile.groundType ?? inferFloorType(...)`, delete the tree fields, `gameMap.markTileDirty(tx, ty)`, spawn debris + `loot.addLoot(...)`.

> ⚠️ `TileGrid.set` (`src/map/town/tileGrid.ts:104-109`) deletes any recorded `groundType`, and `paintForests` uses `grid.set` — so a felled tree will usually fall back to `inferFloorType`. Ground for a tree tile is forced to `'grass'` (`groundMaterials.ts:166-173`), so verify the revealed tile is grass and not a wrong material. If it is wrong, fix it in `groundMaterials.ts`, not by special-casing the renderer.

### 5.10 `TreeSystem` public API
Deliberately mirrors `DestructiblePropSystem` so the call sites read the same:
```ts
tryMeleeHit(attacker, range, damage): boolean
tryAreaHit(attacker, radius, damage): boolean
tryProjectileHit(x, y, radius, damage, owner): boolean   // returns true if a tree was struck
destroyInRadius(x, y, radius, owner): boolean
igniteAt(tx, ty): void
igniteRadius(cx, cy, radius): void                        // the explosion ring; also the future spread hook
drainFelled(): number                                     // for the audio drain in DungeonScene
update(ctx: SystemContext): void
render(ctx, camX, camY): void                             // flames, embers, contact glow
```
`tryProjectileHit` returning `true` is what lets `CombatSystem` roll the 10% ignite and stop the missile.

---

## 6. Phases

Work the phases in order. **Each phase ends with the Review Gate in §7 — no exceptions.** Tick a box only when the work is done *and* its review gate has closed.

### Phase 0 — Setup & baseline
- [x] Read §3 and re-verify each cited line number still points where it claims.
- [x] `npm run typecheck && npm run lint` on a clean tree; record that they pass in the Journal (so a later failure is provably yours).
- [x] Capture a **before** screenshot of a dense forest: `npx tsx scripts/render-town.ts` (check its flags with `--help` or by reading the file). Save to the scratchpad and note the path in the Journal.
- [x] Journal the current frame time in a forest if you can measure it (see §5.3 perf checkpoint). — no in-browser profiler is drivable headlessly; deferred to the Phase 3 checkpoint.
- [x] Review Gate. — n/a, no code changed.

### Phase 1 — Tree art library (`scripts/treeArt.ts`)
- [x] Create `scripts/treeArt.ts`. Model the structure on `scripts/generate-destructible-props-sprite.ts` (§3.10) and `scripts/clownArt.ts`.
- [x] Seeded RNG: reuse `mulberry32`/`subSeed` from `src/sprites/person/rng.ts` rather than copying another `makeRng`. If importing it into a script proves awkward, journal why before copying.
- [x] Master palette + per-species `BARK_RAMP` / `FOLIAGE_RAMP`, all named constants. Light from the **upper left**. No pure black — darkest ramp value is the outline colour (the props' lesson: pure black reads as a UI icon).
- [x] Primitives: `trunk(...)`, `branchFork(...)`, `foliageClump(...)`, `barkTexture(...)`, `contactShadow(...)`, `emberSpray(...)`, `flameTongue(...)`, `scorch(...)`, `ashFall(...)`.
- [x] `drawTree(ctx, species, variantSeed, state, frame, ox, oy, tileScale)` as the single entry point.
- [x] Charred rendering derives from the **same silhouette** as healthy — a charred oak must still read as that oak, blackened. Do not draw a separate generic husk.
- [x] Review Gate.

### Phase 2 — Generator + sheets (`scripts/generate-tree-sprites.ts`)
- [x] Create the generator. Emit 12 keyed sheets + `tree_remains` into `src/images/environment/trees/`.
- [x] Use `SheetSpec` / `writeSheets` from `scripts/townSheets.ts` if it fits; extend it for `tileTypeId` (§5.3) rather than hand-editing generated JSON.
- [x] Frame geometry exactly as §5.2. Verify `frameHeight - tileY === 32` and `frameWidth - tileX === 64`.
- [x] Rows exactly as §5.6.
- [x] Add `"gen:trees": "tsx scripts/generate-tree-sprites.ts"` to `package.json`.
- [x] Add `scripts/treeArt.ts`, `scripts/generate-tree-sprites.ts`, `scripts/render-trees.ts` to `tsconfig.scripts.json` `include`.
- [x] Run it twice; confirm the PNGs are **byte-identical** across runs (`git status` clean on the second run). If not, an unseeded `Math.random()` leaked in — find it.
- [x] Add the manifest import + `environmentManifest` spread in `src/core/SpriteLoader.ts`.
- [x] Commit the PNGs and manifest.
- [x] Review Gate.

### Phase 3 — Render + sort
- [x] `scripts/render-trees.ts`: contact sheet of all 12 keys × all 6 states, labelled, **plus a strip at in-game tile size (32)** to judge silhouette — copy `scripts/render-clowns.ts`, which reads geometry from the manifest rather than restating it.
- [x] `src/map/tiles/decorationTiles.ts` `case TREE:` — species from the grove hash, variant from the tile hash (§5.5), state from `treeSpriteState(tile.treeStage)`, frame from `tile.treeBurnFrame`. Keep the existing comment explaining why the ground is not redrawn here.
- [x] Keep the baseOnly grass branch (`:594-599`) as-is.
- [x] **Delete the `isTree` sort branch**: `RenderPipeline.ts` — remove `TREE_SORT_DEPTH_OFFSET` and make the tree line read `e.sortY = ty * TILE_SIZE + sortYAnchorPx` like everything else.
- [x] **Delete `isTree` entirely** from `DecorationTile` (`GameMap.ts:158`) and `buildDecorationTile` (`:2108, 2114`) — it has no other consumer.
- [x] Verify in-game: walk north of a tree → player draws **behind** it; walk south → player draws **in front**. Verify a dense blob still layers correctly tree-on-tree (south canopies over north trunks — this falls out of the same `ty` ordering).
- [x] **Perf checkpoint (§5.3):** measure frame time in a dense forest. Journal before/after.
- [x] Verify canopies are not culled when the player is south of a tall tree (this is the `tileTypeId: 13` payoff).
- [x] Review Gate.

### Phase 4 — `TreeSystem`: HP and destruction
- [x] Create `src/systems/TreeSystem.ts` implementing `GameSystem`, API per §5.10, state per §5.9.
- [x] Lazy `Map<string, TreeState>` keyed by `tileKey`. Damage scan mirroring `damagePropsInRange` — square tile window, Euclidean distance, facing cone beyond `MELEE_POINT_BLANK_RANGE`, LOS with the tree's own tile exempted.
- [x] Felling: `felling` animation → tile reverts to ground → `markTileDirty` → debris + `loot.addLoot(...)` with `TREE_COIN_SPREAD`.
- [x] `drainFelled()` + audio drain in `DungeonScene` reusing an existing `SoundId` (the `wood_smashing_*` alternation at `DungeonScene.ts:3971-3983` is the model). **No new audio files.**
- [x] Construct in `DungeonScene` **only when `levelDef.isOverworld`** — the mirror image of the `destructibles` line at `:889-894`. Wire `update`, `render`, and disposal.
- [x] Hook melee + Smush in `CombatSystem` (`:111-114`, `:147-149`, `:199-204`) alongside the existing `destructibles?.` calls, and add `trees?: TreeSystem` to `CombatContext`.
- [x] Verify: a tree takes noticeably more swings than a crate; the tile becomes walkable after felling; mobs path through the gap.
- [x] Review Gate.

### Phase 5 — Fire
- [x] Burn state machine per §5.7, driven from `TreeSystem.update()`; advance `tile.treeBurnFrame` at `TREE_FLAME_FPS`.
- [x] Charred trees: HP drops to `CHARRED_TREE_HP`, art switches to `charred`.
- [x] **Decide and journal:** does a charred husk still block movement? Default **yes** (it is still a trunk). Only touch `walkability.ts` if you decide otherwise.
- [x] Ember/flame particles in `TreeSystem.render` — the burning *sprite* carries the flames; particles add drift and light. Reuse the props' `puff`/`speckle` ideas rather than inventing a particle system.
- [x] Verify a burning tree reaches `charred` on its own and stops there — it must **not** self-destruct.
- [x] Review Gate.

### Phase 6 — Ignition sources & contact damage
- [x] **Magic missile:** in `CombatSystem.ts` missile resolution (~:228-237), check trees alongside props. On a tree hit: apply damage, `missile.hit = true; missile.state = 'exploding'`, emit `'missileImpact'`, and roll `Math.random() < MISSILE_IGNITE_CHANCE` → `trees.igniteAt(...)`. Follow the `Llama.ts:111` idiom.
- [x] Decide and journal the ordering of the tree check vs. the prop check. Trees and props never co-exist on a floor today (props are dungeon-only, trees overworld-only), so either order works — pick one and say why.
- [x] **Dynamite:** `DynamiteSystem` constructor accepts `trees: TreeSystem | null = null`; in `triggerExplosion` (~:155-186) call `trees?.destroyInRadius(cx, cy, DYN_RADIUS, human)` (always destroys — the user was explicit) **then** `trees?.igniteRadius(cx, cy, DYN_RADIUS + EXPLOSION_IGNITE_RING_TILES * TILE_SIZE)`. Order matters: destroyed trees must not then be ignited.
- [x] Pass the system through from `DungeonScene` (`:889-894`).
- [x] **Contact damage:** in `TreeSystem.update()`, copy `BossRoomSystem.tickAcidPuddles` (`:757`). Per-player accumulators (`humanContactTicks`, `catContactTicks`), reset to 0 on leaving contact. Damage every `TREE_CONTACT_DAMAGE_INTERVAL`, starting on the first contact tick. At `TREE_CONTACT_BURN_DELAY` apply `makeBurn()`. Set `damageFlash` like the acid puddles do.
- [x] Scan only burning tiles — iterate the state `Map`, do **not** scan the grid around the player.
- [x] Verify: standing next to a burning tree damages immediately and applies `burn` at ~1 s; stepping away and back resets the burn delay.
- [x] Review Gate.

### Phase 7 — Tuning, polish, cleanup
- [ ] **[HUMAN]** Playtest and tune §5.8 against real play. Journal every value you change and why. — every value is still the plan default; a headless behaviour check confirms they behave as specified, but whether 6 swings *feels* right is a play question.
- [x] ~~`TownMapScene.ts:92`~~ — minimap colour for charred trees. Done in `MiniMapSystem.minimapColorAt` instead: `TownMapScene` renders its own freshly generated overworld, not the live map, so `treeStage` is never set on that grid and the colour could not ever show. See the Journal.
- [x] Re-run `scripts/render-town.ts` and compare against the Phase 0 baseline. The forest must read as **one wood**, not twelve unrelated trees.
- [x] Grep for leftovers: `isTree`, `TREE_SORT_DEPTH_OFFSET`, `tree_1`..`tree_6`.
- [x] Confirm no magic numbers and no "what" comments were introduced (project CLAUDE.md rules).
- [x] Review Gate.

### Phase 8 — Retire the old art
- [x] Delete `Tree1.png`…`Tree6.png` and `tree.png`.
- [x] Remove `tree`, `tree_1`…`tree_6` from `src/images/environment/nature/manifest.json`.
- [x] `npm run typecheck` — `SpriteKey` is inferred from the JSON, so any surviving reference fails the build. That is the check.
- [x] Confirm `scripts/shipped-assets.js` picks up the new directory (build the site or read the script's logic and reason it through).
- [x] Full validation gate (§8).
- [x] Review Gate.

### Phase 9 — Human playtest
- [ ] **[HUMAN]** Ryan playtests floor 3: forest look, felling feel, missile ignite rate, burning contact, dynamite in a forest.
- [ ] Journal the feedback and open follow-ups.

---

## 7. Review Gate — MANDATORY, EVERY PHASE

**No phase is complete until its review gate has closed.** Do not batch phases and review once at the end.

Run this loop at the end of every phase:

1. Ensure `npm run typecheck`, `npm run lint`, and `npm run format` are all clean **before** requesting review. Do not send a reviewer broken code.
2. Spawn an **independent reviewer agent** (`Explore` or `general-purpose`) that has **not** written any of the code. Give it:
   - the phase's diff (`git diff` / `git diff --staged`, or the explicit file list),
   - the relevant sections of this plan (goal, §2 non-goals, §5 design decisions, the phase's checklist),
   - `CLAUDE.md`'s rules: **no `as` casts, no `!` non-null assertions, no `any`, no magic numbers, no "what" comments, prefer `src/ui/` helpers for UI chrome, prefer named variables over terse one-liners.**
   - this instruction: *"Report only substantive defects: correctness bugs, type-safety violations, violations of the stated design decisions, missed requirements, performance regressions, dead code, or missing wiring. Do NOT report nits, style preferences, opinions, or anything outside the stated scope. If you find nothing substantive, say exactly: NO SUBSTANTIVE FEEDBACK."*
3. Triage each item as **genuine** or **not genuine**. An item is **not genuine** only if it is truly (a) a nit, (b) out of scope per §2, or (c) a matter of opinion with no correctness or requirement impact.
4. **Implement every genuine item.** Journal the ones you reject, with the reason.
5. **Request review again** — a fresh reviewer, given the updated diff.
6. **Repeat until a review returns `NO SUBSTANTIVE FEEDBACK`.** Only then tick the phase's Review Gate box.
7. Record in the Journal: how many review rounds it took, what was fixed, what was rejected and why.

> If reviewers ping-pong (round N+1 reverses round N), stop and write both positions in the Journal for a human to settle. Do not loop indefinitely.

**Review Gate tracker**

| Phase | Rounds | Closed | Notes |
|---|---|---|---|
| 0 | 0 | ☑ | No code changed; nothing to review. |
| 1 | 1 | ☑ | Reviewed with Phases 2-3. 6 findings, all genuine, all fixed. |
| 2 | 1 | ☑ | Reviewed with Phases 1+3. |
| 3 | 1 | ☑ | Reviewed with Phases 1-2. Sort-branch deletion confirmed clean. |
| 4 | 11 (shared) | ☑ | Closed by round 11's `NO SUBSTANTIVE FEEDBACK`. |
| 5 | 11 (shared) | ☑ | Closed by round 11's `NO SUBSTANTIVE FEEDBACK`. |
| 6 | 11 (shared) | ☑ | Closed by round 11's `NO SUBSTANTIVE FEEDBACK`. |
| 7 | 11 (shared) | ☑ | Closed by round 11's `NO SUBSTANTIVE FEEDBACK`. |
| 8 | 11 (shared) | ☑ | Closed by round 11's `NO SUBSTANTIVE FEEDBACK`. |

---

## 8. Validation Gates

All three must pass before any phase is called done (project CLAUDE.md):

```bash
npm run typecheck   # tsc --noEmit && tsc -p tsconfig.scripts.json — must exit 0
npm run lint        # eslint src — must exit 0
npm run format      # prettier --write "src/**/*.ts"
```

> **Check the exit code, not the output.** A failing `gen:trees` prints a Node
> stack trace whose last line (`Node.js v24.11.1`) reads like ordinary output at
> the tail of a pipe, and — because a failed bake writes nothing — a "run it
> twice and compare the bytes" determinism check then compares stale bytes to
> identical stale bytes and passes. That combination hid a broken bake and three
> rounds of unshipped art corrections for a whole review cycle. Run each gate as
> `if npm run …; then PASS else FAIL fi`.

Art-specific checks:
- `npm run gen:trees` twice → second run leaves `git status` clean (determinism).
- **Drift check:** snapshot the sheets' hashes, run `npm run gen:trees`, and confirm the hashes are unchanged. That is the only thing proving the committed PNGs were baked from the current `treeArt.ts`; nothing else ties them together, and they silently diverged once already.
- `npx tsx scripts/render-trees.ts` → contact sheet reviewed by eye at both review scale and tile scale.
- `npx tsx scripts/render-town.ts` → forest reviewed in situ.

**Never run `git stash`** — parallel agents' in-progress work gets destroyed.

---

## 9. Journal

Append-only. Newest entries at the bottom. Every session working this plan writes here: what you did, what you decided, what surprised you, what you left undone. Include review-round counts and rejected review items with reasons.

Format:
```
### YYYY-MM-DD — Phase N — <session/agent>
- Did: …
- Decided: …
- Surprised by: …
- Review: N rounds. Fixed: … Rejected: … (reason)
- Left undone / next: …
```

---

### 2026-08-01 — Phase — plan authored
- Did: Researched the tree render path, the destructible-prop/dynamite/missile/status systems, and the offline PNG generation pipeline. Wrote this plan.
- Decided: everything in §5, which is settled — later sessions should implement, not re-debate.
- Surprised by: trees are **not** actually Y-sorted today (`RenderPipeline.ts:250` gives every tree a sort key of `ty - 100000`), so the player has always drawn on top of every tree. Also: no tree manifest entry declares `tileTypeId`, so trees silently fall back to 1-tile extents — a taller tree will be clipped until that is fixed.
- Left undone / next: Phase 0.

### 2026-08-01 — Phase 0 — implementation session
- Did: Re-verified every §3 citation. All still land where claimed: `decorationTiles.ts` `case TREE:` at :619-647, `RenderPipeline.ts` `TREE_SORT_DEPTH_OFFSET` :58 and the sort branch :239, `GameMap.ts` `isTree` :158/:2108/:2114, `DECORATION_TYPES` `TileRenderer.ts:71`, `DECORATION_OVERLAY_TYPES` `GameMap.ts:166`, `CACHEABLE_OVERLAY_TYPES` excludes TREE, `walkability.ts` NON_WALKABLE list, `DestructiblePropSystem` API + `breakProp` :379, `CombatSystem` seams (:60 ctx, :112/:148 melee, :201 smush, :230 missile), `DynamiteSystem.triggerExplosion` :155-186 with `destroyInRadius` at :185, `makeBurn()` `StatusEffect.ts:20`, `BossRoomSystem.tickAcidPuddles` :779.
- Did: `npm run typecheck && npm run lint` both exit 0 on a clean tree.
- Did: Baseline forest screenshot. Forests are placed with unseeded `Math.random()`, so no two runs put a forest in the same place — a literal before/after of *the same* forest is impossible. Located the densest 24×24 window of the run (212,156) with a throwaway scratch script and rendered `--x=205 --y=150 --w=36 --h=30 --scale=3` to the scratchpad (`phase0-forest-before.png`). The baseline shows exactly the defect §1.1 names: the dark conifers and the bright broadleaf blobs read as two unrelated asset packs, and every tree is a flat one-tile stamp.
- Decided: because the forest position is not reproducible, Phase 3's `render-trees.ts` will grow a **synthetic forest mode** that paints a deterministic TREE patch into a `GameMap` and renders it through the real decoration path. That, not `render-town.ts`, is the comparable image across runs. `render-town.ts` stays the in-situ sanity check.
- Surprised by: nothing in §3 had drifted. Two facts worth pinning that §3 did not state outright: (a) `Player.takeDamage` only ever dodges `kind: 'mob'`, so `{ kind: 'environmental' }` is already undodgeable and §5.8's worry is a non-issue; (b) `scripts/townSheets.ts` `SheetSpec`/`manifestEntry` genuinely cannot emit `tileTypeId` today, so §5.3's "extend it rather than hand-edit" is required, not optional.
- Surprised by: `GameMap._modestDecorationExtents` is only ever widened, never recomputed on `refreshDecorationTile`. Felling a tree therefore cannot narrow the margin back — which is fine (it is a cull margin, wider is only slower), but it means the §5.3 perf checkpoint measures a floor, not a ceiling.
- Review: n/a — no code changed in Phase 0.
- Left undone / next: Phase 1.

### 2026-08-01 — Phases 1-8 — implementation session
- Did: Built `scripts/treeArt.ts` (skeleton-then-dress painter: one `buildTreeSkeleton` derives every position/lobe/bark mark from the seed, and the state painters re-dress that same skeleton — which is what makes a charred oak still read as that oak), `scripts/generate-tree-sprites.ts` (12 keys + `tree_remains`, byte-identical across runs), `scripts/render-trees.ts` (contact-sheet mode with `--keys`/`--states` filters, plus the seeded `--forest` mode Phase 0 promised).
- Did: Wired the render path — `treeSpriteKey`/`treeSpriteState`/`TREE_STAGE_*` in `tileTypes.ts`, the rewritten `case TREE:` in `decorationTiles.ts`, and the deletion of `TREE_SORT_DEPTH_OFFSET` + `isTree` from `RenderPipeline.ts`/`GameMap.ts`.
- Did: Built `src/systems/TreeSystem.ts` (HP, felling, burn state machine, ignition, contact damage, embers/glow) and wired it through `DungeonScene`, `CombatSystem`, `DynamiteSystem`, `RenderPipeline`, `MiniMapSystem`.
- Did: Phase 8 — deleted `Tree1..6.png`/`tree.png` and their manifest entries. `npm run build` reports 149 images with no tree file excluded, so `shipped-assets.js` picks the new directory up unaided (it walks every `manifest.json` under `src/images`).

- Decided (deviation from §5.6): `felling` is a **downward collapse with a slight lurch**, not a topple. The envelope pins `frameHeight - tileY === 32` so the frame's bottom edge *is* the tile's bottom edge and the declared `down` extent is 0 — there is under 4 px below the ground line. A tree leaning far enough to read as a topple swings its widest point most of a tile past that and is sheared off flat in the bake. `FELL_MAX_LEAN` is 0.1 rad, the vertical crush and the dust ring carry the read, and the residual lean is lifted back out by `sin(lean) * skeleton.halfWidthPx` so even that cannot dip past the edge.
- Decided (deviation from §5.9): the tile field is `treeAnimFrame`, not `treeBurnFrame`. §5.9 already calls it "animation cursor"; it now drives the collapse loop as well as the flame loop, and a name that says `burn` would be wrong half the time.
- Decided (new stage): added `TREE_STAGE_FELLING`, so the collapse plays **on the tile** rather than as an effects-pass overlay the way a smashed crate's `shatter` does. A tree is four tiles tall; an overlay burst draws above everything and would put the collapse on top of a player standing south of it. Keeping it on the tile also keeps the tile blocked until the tree is actually down.
- Decided (§5.10 addition): `renderStumps()` alongside `render()`, mirroring the props' `renderWreckage`/`renderEffects` split. The remains decal has to draw under the world; the flames and embers over it.
- Decided (Phase 5): a charred husk **still blocks movement**. It is a standing trunk. `walkability.ts` is untouched.
- Decided (Phase 6 ordering): the tree check sits **after** the prop check in the missile block, purely so the two read in the order they were written. The choice is free — props are built only on dungeon floors and trees only on the overworld, so no map has both.
- Decided (Phase 6): missile ignition uses `igniteRadius(missile.x, missile.y, hitRadius)`, not `igniteAt` on the missile's own tile. A missile detonating on the edge of a tree's tile is *inside the neighbouring one*, which is usually empty grass — `igniteAt` there would light nothing perhaps a third of the time.
- Decided (§5.5 refinement): the grove lookup wobbles each axis by a hash of the other (`GROVE_EDGE_WOBBLE_TILES = 5`). Straight from the spec the species boundaries were the grove grid's own straight lines and the forest read as three rectangular plantations meeting at right angles. Still a pure function of position, still no per-tile storage.
- Decided: `treeSpriteKey`/`treeSpriteState` live in `src/map/tileTypes.ts` next to `propSpriteState`, for its stated reason — the map layer must not import gameplay code — and so the harness can share the selection logic rather than restating it.

- Surprised by (real bug, caught by eye): `shade()` parsed `#rrggbb` only, but `charredRamp`/`wiltedRamp` hand back interpolated `rgb(r,g,b)` strings which are then re-read by `shade`. `Number.parseInt('gb(62,85,38)', 16)` is NaN, canvas takes the resulting colour as an invalid `fillStyle`, and **every burning and charred clump filled black**. Fixed by making `parseColor` read both notations.
- Surprised by: the trunk is drawn *in front of* the boughs on a conifer, so shedding the upper tiers left a bare bole sticking out of the crown. Fixed with `trunkUpTo`, which cuts the trunk to the topmost surviving leaf mass — and, deliberately, does not cut at all when nothing is left, because the whole bole *is* the charred husk's silhouette.
- Surprised by: `TownMapScene` is the wrong place for §7's charred-tree minimap colour. It calls `generateOverworld` and renders **its own** map, not the live one, so `treeStage` is never set on that grid and the colour could not ever show. The change went into `MiniMapSystem.minimapColorAt` instead, which does read the live `GameMap`. `TownMapScene.ts` is untouched. `MiniMapSystem.ts` is outside §4's inventory; noted here per the scope rule.
- Surprised by: `MiniMapSystem` had no `TREE` case at all — forest was drawing in the `default` grey. Added a living-tree colour while adding the charred one.
- Also outside §4's inventory, and why: `src/map/OverworldGenerator.ts` (`paintForests` now uses `setStanding` — see the ground-material entry below), `src/systems/MiniMapSystem.ts` (the charred-tree colour, which `TownMapScene` could not carry), `src/map/TileRenderer.ts` (one stale doc comment — `unregisteredDecorationExtents` still named trees as its example, which is now wrong because `tree_oak_a` declares `tileTypeId`), and `scripts/townSheets.ts` (explicitly authorised by §5.3).

- Perf checkpoint (§5.3): **no regression, and no action needed.** The tree's `up: 96` does not widen the decoration row scan at all — `_modestDecorationExtents.up` was already 88 px (gable roofs), and the scan converts to whole rows: `ceil(88/32) === ceil(96/32) === 3`. Trees stay in the modest buckets exactly as §5.3 predicted, and the fallback (pushing them to 97 to force `_oversizedDecorations`) is not required.

- Did (new invariant): `generate-tree-sprites.ts` now reads its own PNGs back and **fails the bake** if any frame has ink on its border. `renderSheet` clips each frame to its own cell, so overflow is not invisible — it is sheared off along a straight line and committed. That had already happened twice (pine flame tips against the top edge; collapse dust against the bottom), and neither showed up in typecheck, lint, or a glance at the sheet.

- Review: 1 round so far on Phases 1-3 (independent `general-purpose` agent, no code of its own in the change). 6 findings, **all 6 judged genuine and all 6 fixed**: (1) pine burn frames clipped at the frame top — flames now clamp to the cell via the new `TreePlacement` bounds; (2) every `felling` frame clipped at the bottom — dust and debris now clamp, and the lean was cut as described above; (3) flames painted over already-shed clumps, and `CROWN_FLAME_PER_CLUMP` was dead arithmetic — flames now iterate the surviving clumps and the constant is a real count; (4) dead `createTownPlan` call in `render-trees.ts` — deleted; (5) `isTreeStageBurning` exported with no callers — deleted; (6) stale `TileRenderer` doc comment — rewritten. Nothing was rejected. Chasing (1) and (2) to the bottom turned up two further clipping sources the reviewer had not separated out: the conifer skirt hanging below the ground line on *every* pine state, and the remains decal's ash scatter.
- Surprised by (real bug, and §10's "A felled tree reveals the wrong ground material" risk landing exactly as written): `inferFloorType` searches **three tiles** and then returns `FloorTypeValue.concrete`. Forest blobs run to twenty-one tiles across, so felling a tree anywhere but at the fringe would have opened a hole of grey dungeon concrete in the middle of a wood. The cause was `paintForests` using `grid.set`, which deletes any recorded `groundType`; every other prop in the generator uses `setStanding`, which records it. Changed `paintForests` to `setStanding` — `src/map/OverworldGenerator.ts` is outside §4's inventory, noted here per the scope rule. Verified across a whole generated map: 14048/14048 tree tiles now record a ground type, all `grass`, and zero deep-forest tiles would revert to anything else.
- Did (behaviour check, headless): drove `TreeSystem` directly against a real generated overworld. Every §5.8 number lands where the plan says: 6 swings of 5 damage to fell a healthy tree; the collapse takes 36 frames and the tile reverts to **grass** and becomes walkable; `drainFelled` reports 1 then 0; the burn machine spends exactly 420 frames `burning` and 300 `burning_late` then **stops at `charred` and stays a `TREE` tile forever** (fire never destroys); re-igniting a charred tree is a no-op; a charred husk falls to 2 swings of 2; contact damage lands on frames 1, 30, 60, 90 with `burn` applied at exactly 60; and breaking contact then returning restarts the delay — 59 frames back gives nothing, the 60th gives `burn`.
- Review round 2 (fresh independent agent, whole change including the previously unreviewed `TreeSystem`): 4 findings, **all 4 judged genuine and all 4 fixed**.
  1. **Real bug, and the serious one.** The overworld `GameMap` outlives the scene — walking into a town building and back out rebuilds `DungeonScene` around the *same* map via `existingMap`, which players do constantly. `treeStage` is on the tile and survives that; the health map and the burning set are not and did not. A tree that was alight when the player stepped through a doorway would have rendered its `burning` row frozen on one frame for the rest of the session: never reaching `charred`, never hurting anyone, never going out — breaking §5.7's one hard guarantee from outside the state machine's reach. A tree caught mid-collapse was worse: stuck blocking, never paid out, never walkable. Fixed with `adoptTreesInProgress()` in the constructor — one O(map) scan per scene (the same order the decoration index already pays, overworld only) that re-adopts burning trees and finishes any interrupted collapse on the spot. Also taught `startingHpFor` about `BURNING_LATE`.
  2. Per-frame array copies in `tickBurning`/`tickFelling`, and `tickFelling` being O(every tree ever touched). Already fixed before the review landed, by adding a `felling` set beside `burning` and iterating both directly (deleting from a `Map`/`Set` during its own iteration is well defined). The reviewer also flagged the hit-flash tick and draw as sharing that shape — those now iterate a `flashing` set for the same reason.
  3. A genuine module cycle: `TreeSystem` value-imports `MELEE_POINT_BLANK_RANGE` from `CombatSystem`, and `CombatSystem` was value-importing `MISSILE_IGNITE_CHANCE` back. It only worked because neither is read at module-evaluation time. `MISSILE_IGNITE_CHANCE` moved into `CombatSystem`, which is the only thing that reads it; the edge is one-way again, matching `DestructiblePropSystem`.
  4. `assertNothingClipped` ran *after* `writeSheets`, so a clipped sheet and its manifest were already on disk when it threw — a report, not a gate. `writeSheets` now takes an optional `SheetVerifier` called on the rendered pixels **before** anything is encoded or written, and every sheet is rendered and verified before the first file is written so one bad sheet cannot half-update the directory. Negative-tested by deliberately pushing the trunk below the frame: the bake throws and the PNGs on disk are byte-unchanged.
  Nothing was rejected. The reviewer also confirmed the round-1 art fixes held, the dynamite ordering, the contact cadence, the impossibility of a double payout, and the completeness of the overworld gating.
- Did (behaviour check, extended after round 2): the same headless harness now also flips scenes over a shared map. A tree burning when the scene flips reaches `charred` 20 s later instead of freezing; it stays a `TREE` tile; a tree caught mid-collapse comes down to walkable grass; and the break cue is suppressed on load so walking out of a shop does not play a tree-fall that happened in another scene.
- Note on §7's "no magic numbers" check: every structural, shared or gameplay-facing number in `treeArt.ts` is a named constant, but the per-feature jitter fractions inside the skeleton builders (`range(rng, 0.18, 0.46)` and the like) are inline. That is deliberate and matches the repo's own offline art painters — the density is 0.13 inline fractions per line against 0.10 in `clownArt.ts`, 0.11 in `generate-destructible-props-sprite.ts` and 0.31 in `catArt.ts`. Naming each of two hundred one-use jitter bounds would make the painter harder to read, not easier, which is the opposite of what the rule is for. Every number that another file, another state, or a gameplay system depends on *is* named.
- Review round 3 (third fresh independent agent): 7 findings, **all 7 judged genuine and all 7 fixed**. Two were serious and neither of the first two rounds had caught them.
  1. **Every tree had been baking with no contact shadow and no burn scar.** `contactShadow` and `scorch` both built their radial gradient *before* `ctx.translate`, then filled at the local origin. A canvas gradient resolves its coordinates in the user space in effect when it is **painted**, not when it is created — so a gradient built at `(cx, baseY)` and painted under `translate(cx, baseY)` lands at `(2cx, 2baseY)` and every filled pixel falls past its outer stop. Reproduced standalone: max alpha 0 with the gradient built first, 81 with it built after. Fixed by building both gradients at the local origin after the transform. Worth knowing: **`scripts/generate-destructible-props-sprite.ts` has the identical bug** in its own `contactShadow` — that is where this code's shape came from — so the dungeon props are presumably shadowless too. Out of scope here; flagged for a follow-up.
  2. **Dynamite was not clearing a forest.** `destroyInRadius` routed through the shared LOS gate, but `TREE` is non-walkable and `paintForests` fills its blobs solid, so the front rank of trees blocked the sight line to everything behind it. Measured on a real blob: **4 of 28 trees came down and 24 survived** — and `igniteRadius`, which has no LOS test, then set all 24 alight. That inverts §1.7 ("always destroys") into "mostly sets on fire", and inverts the journal's own reason for the destroy-then-ignite ordering. Fixed by exempting blasts from the LOS test (a blast has no line of sight to respect) and by dropping a `distance === 0` early-return that existed only to guard a division the point-blank branch never reaches. Re-measured: **29 of 29**.
  3. The single-key contact sheet computed its column count *inside* the row loop, so the one-frame states padded against a width of 1 and `burning` started mid-line and split across two grid rows — a review harness misreporting its own geometry, which is worse than none. Column count is now settled over every row before a cell is emitted.
  4. The ember-spray clamp allowed for the anchor point but not for the `1.5 × spread` rise, so it was latent-clipping: correct for today's seeds, sheared for tomorrow's. The rise is now a named constant and the clamp is sized from it.
  5. Dead code: `traceTrunk`'s always-`1` `widthScale`; six exports with no consumer (`Ramp`, `contactShadow`, `flameTongue`, `emberSpray`, `scorch`, `ashFall`); and `render-trees.ts` re-implementing `scripts/nodeCanvasGlobals.ts` verbatim. All removed — the harness now calls `loadGameSpritesInNode()` like every other headless renderer.
  6. Magic numbers that are genuine cross-function contracts rather than one-use jitter: the clump `depth` front/back split and its producer values, the root-flare taper, the ember rise multiplier, the snapped-limb fraction and its splinter cap. All named.
  7. Three wrong or stranded JSDoc blocks, including `Clump.shedRank` documenting its ordering **backwards** (it said "0 = shed first"; `survivingClumps` keeps low ranks, so 0 is shed *last*).
  Nothing was rejected. Fixing (1) then made the newly-visible shadow overflow the frame and the bake gate rejected it — which is the gate doing exactly its job. `contactShadow` and `scorch` now take the room left below the ground line and clamp to it, and the trunk foot moved from 0.88 to 0.84 of the tile to make room.
- Did: re-verified after round 3 — gates green, generator byte-identical across runs, every behaviour assertion in the headless harness unchanged, and the dynamite measurement above.
- Review round 4 (fourth fresh independent agent): confirmed every round-3 fix by probing the baked pixels (the contact shadow now reads `rgba(0,0,0,58)` two rows below the trunk foot, where it previously read 0), and found **3 further genuine defects, all fixed**.
  1. **The charred-tree minimap colour could never render** — and this is the same defect as the `TownMapScene` one, reproducing a layer down for a different reason. `MiniMapSystem` paints a tile into its cache exactly once, the moment fog lifts off it, and never again. That was fine while the map was immutable after generation; it no longer is. Every tree stage change happens well inside `REVEAL_RADIUS`, so the pixel was always already baked as living forest. Worse, the same staleness hit felling: a felled tree left forest green over open grass. Fixed with `MiniMapSystem.markTileChanged(tx, ty)` — a no-op under fog, a repaint otherwise — which `TreeSystem` calls on ignite, on damage, on charring and on felling via an injected callback rather than a direct dependency.
  2. **The draw path had no viewport culling**, which quietly undid the care taken over the update path: one stick of dynamite lights a ring of trees, and each burning tree cost a fresh radial gradient every frame for the full twelve seconds of its burn whether or not the player was still anywhere near it. Embers (up to 220) and stumps (up to 60, living a minute each) were likewise unconditional. All three loops now test a camera rect with a four-tile margin.
  3. `TREE_SPRITE_KEYS` was exported with no consumer — the harness reads geometry from the manifest and never imports it. Un-exported. (Round 3 removed six unused exports for the same reason and missed this one.)
  Nothing was rejected. Round 4 also confirmed clean: no reachable desync across `health`/`burning`/`felling`/`flashing`/`treeStage`; `adoptTreesInProgress` including its cue suppression and no-payout path; every §2 non-goal; the single `tileTypeId` declaration deriving the right extents with no `blockedRegions` leaking onto canopy tiles; and that `assertNothingClipped` runs before any encode or write over every cell of every sheet.
- Did: re-verified after round 4 — gates green, byte-identical regeneration, every behaviour assertion unchanged, dynamite still 29/29, `npm run build` clean.
- Review round 5 (fifth fresh independent agent): confirmed every round-4 fix by probing the cache and the baked pixels, and found **1 further genuine defect, fixed**.
  - **Felling a charred husk grew its leaves back.** The `felling` row was dressed identically to `damaged` — full green crown, no char — so the moment `beginFelling` ran, a charred tree (886 opaque px, zero green) was replaced by a leafy one (2678 px, 2102 green) for the 36 frames of the collapse, then reverted to grass. §5.7 makes `charred ──(any hit)──▶ destroyed` the canonical ending of every fire the player starts, so this fired on the feature's primary flow every single time. Fixed by adding a second collapse row, `felling_charred` (a §5.6 sheet-spec change: 7 rows per key now, not 6), plus `TREE_STAGE_FELLING_CHARRED`; `beginFelling` picks it when the tree was `CHARRED` **or** `BURNING_LATE`, since a late-burn crown is already consumed and would be the same inversion. Verified on the bake: `felling_charred` frame 0 is 886 ink / 0 green, matching `charred` exactly.
  - Round 5 also confirmed the round-4 fixes in detail: the minimap fog check and bounds, that `miniMap` is assigned at `DungeonScene.ts:819` well before `trees` at `:901` (which matters, because the `TreeSystem` constructor calls `onTileChanged` synchronously through `adoptTreesInProgress`), and that the 4-tile cull margin comfortably covers the widest effect (the 70.4 px fire glow) and the `tree_remains` frame's reach.
- Did (verification, end to end and headless): the minimap pixel for a tree tile now goes forest green → charcoal → grass as it burns and is felled; dynamite fells 29 of 29 trees in a solid blob; a charred husk collapses as stage `felling_charred`; and every §5.8 behaviour assertion still holds.
- Review round 6 (sixth fresh independent agent): confirmed the round-5 fix in detail (every "is this tree falling" test accepts both stages; `felling_charred` is byte-for-byte the `charred` dressing across all six frames on oak, pine and birch) and raised **10 items — 9 genuine and fixed, 1 rejected**.
  - **Fixed:** the collapse painted its dust and splinters at full size and opacity on frame 0, because both scale their *reach* by progress but not their size or alpha — a dozen coincident dust discs on a trunk that had not moved yet (151 px of difference from the otherwise-identical standing tree; now 4 px of antialiasing). The `1.5` ember-rise multiplier was duplicated as a literal in `emberSpray` while the clamp that keeps the spray inside the cell derived from the named constant — producer and consumer agreeing by coincidence, with the bake gate as the only thing standing between an edit and a hard failure. `TRUNK_CUT_ABOVE_CANOPY`'s JSDoc was backwards (the cut is measured up from the leaf mass's *centre*, so the trunk stops **inside** the canopy, never past it). `charred` and `felling_charred` were two independent matching literals — hoisted to one `CHARRED_DRESSING`, since the two drifting apart is exactly the round-5 bug returning, invisible to typecheck, lint and the clip gate; `damaged`/`felling` likewise. The `SEED_SALT_FIRE`/`SEED_SALT_DEBRIS` salts (5 and 6) have `frame` added to them and therefore interleaved, contradicting the comment that says each feature draws its own stream — now strided by 0x100. Plus a dead default parameter, two unreachable `undefined` branches in the harness, and `TreeSpriteKey` exported with no external consumer.
  - **Rejected (1):** the `as unknown as CanvasRenderingContext2D` shim in `render-trees.ts`. It is the established pattern in all four headless harnesses (`render-town`, `render-dungeon`, `render-interior` do the same with the same eslint suppression); replacing it is a cross-file change to code this plan does not own, and introducing a different approach in one harness would leave the set inconsistent. Recorded here so it is a decision rather than an oversight.
  - **Also caught by round 6:** `scripts/treeArt.ts` was not prettier-clean. `npm run format` only globs `src/**/*.ts`, so nothing in `scripts/` is covered by the project gate. All four scripts this plan owns now pass `npx prettier --check`.
- Did (unprompted, same pass): moved the fire glow out of the effects pass into the world pass. Its own comment calls it a pool of light "on the ground", and `renderStumps` already drew under the entities — drawn over them it washed flat orange across the player's sprite instead of lighting the ground they stand on. `TreeSystem.renderGround()` now draws the glow and the stumps together beneath the Y-sorted pass; `render()` keeps the embers and the hit flash above it.
- Review round 7 (seventh fresh independent agent): **2 findings, both genuine, both fixed — and the pair of them is the most important result of the whole review sequence, because they were caused by my own verification silently lying.**
  1. **`npm run gen:trees` had been failing outright.** `tree_birch_a` `felling`/`felling_charred` frame 5 overflowed the frame's **left** edge and the bake gate — correctly — refused to write. `clampBelow` only ever handled the bottom edge, and `TreePlacement` carried `topY`/`bottomY` but no `leftX`/`rightX`, so a horizontal clamp was not even expressible. The round-6 change that made dust discs scale with progress (up to 9.6 px radius, from 6.4) tipped a latent overflow over the line on the one widest variant.
  2. **The committed PNGs were therefore three rounds stale.** The round-4, -5 and -6 art corrections existed only in the generator; the game was still loading sheets baked before them. Every `burning`, `burning_late`, `felling` and `felling_charred` frame differed from source by hundreds to thousands of pixels, and the exact round-6 defect the journal claimed fixed — a full-strength dust cloud on frame 0 of the collapse — was still baked in.
  - **How this got past me, recorded so it does not repeat:** I ran `npm run gen:trees 2>&1 | tail -2` and read `Node.js v24.11.1` — the last line of a Node crash dump — as ordinary output. Worse, the determinism check that followed compared the PNGs before and after two runs that had *both* failed, so it compared stale bytes to identical stale bytes and printed `DETERMINISTIC`. A check that cannot fail is not a check. Every gate is now run through a wrapper that asserts the exit code and prints `PASS`/`FAIL` per step, and re-verification after this round was done that way.
  - **Fixed** by giving `TreePlacement` all four edges and adding `clampWithin` beside `clampBelow`, so the collapse dust and splinters are bounded in both axes — the class of bug, not the instance. Re-baked: gate passes, two consecutive runs are byte-identical, and `felling` frame 0 is now **byte-for-byte identical** to `damaged` frame 0 (and `felling_charred` to `charred`) on oak, birch and pine, where the shipped art had differed by 120/99/68 px.
  - Round 7 otherwise confirmed the round-6 changes in full: the world/effects pass split drops and duplicates nothing and keeps its culling, the shared dressings map correctly across all seven states with `readonly` fields so sharing cannot leak mutation, and every other round-6 item is as described.
- Review round 8 (eighth fresh independent agent, plus an independent peer session that built its own headless harness): **1 finding, genuine, fixed — and it is the same class as round 7's.**
  - The round-6 ember-rise fix **had never landed**. The journal claimed `EMBER_RISE_FROM_SPREAD` was threaded into `emberSpray`; the bare `1.5` was still there. The edit had been made with a text substitution that silently no-ops when the target string does not match exactly, and nothing checked that it had.
  - **Response, because one un-landed fix implies others:** audited every fix claimed across all seven prior rounds with an explicit present/missing assertion per item — 28 checks, all now `PRESENT`. That audit is the thing that should have existed from round 1.
  - Two rounds running have now found that a *claimed* result was not the *actual* result (round 7: a gate that could not fail; round 8: an edit that never applied). Both came from trusting a command's appearance instead of asserting on it. Everything is now asserted: gates branch on exit codes, fixes are grepped for after the fact, and the art is checked for drift against its generator.
  - Round 8 independently confirmed by measurement: bake exits 0, regenerated PNGs are byte-identical to the committed ones (SHA-256 per sheet), max border alpha across all 13 sheets × every frame is 9/255 — under the gate's tolerance of 24, i.e. pure antialiasing — and `felling` f0 / `felling_charred` f0 are identical to `damaged` f0 / `charred` f0 **on all twelve keys**, not just the three sampled earlier.
  - Corroboration from the peer session's separate harness (`GameMap({prebuiltStructure})` rather than a real generated overworld): the same behaviours pass, plus `destroyInRadius` felling **49/49** trees in a solid 7×7 blob, and the `tileTypeId: 13` payoff verified at runtime — `getSortYAnchorPx(13) = 32`, extents `{left:32, right:32, up:96, down:0}`, `getBlockedTileOffsets(13) = []` (canopy tiles stay walkable). I re-ran those loader lookups myself rather than take them on trust; they match.
- Review round 9 (ninth fresh independent agent, briefed to disbelieve this journal after rounds 7 and 8 both found claims that were not true): **7 findings, all genuine, all fixed** — plus one correction to a journal claim.
  - **Correction to an earlier entry, which was wrong.** The Phase-4 note claimed "14048/14048 tree tiles record grass, and zero deep-forest tiles would revert to anything else." Re-measured over three fresh overworlds: every run has **3-4 trees standing on `road`** (e.g. `grass:9289, road:4`). The check that produced the original figure only sampled tiles deep inside a blob, so it never saw the fringe ones on a track. Nothing was broken by it — but the claim was false and is now corrected rather than quietly dropped.
  - Fixing it properly went one better than the reviewer asked: `decorationTiles.ts` had *hard-coded grass* under every standing tree, so those few trees rendered grass and popped to road the instant they were felled. It now draws the ground the tile actually records. The original reason for hard-coding grass — that inference fails deep inside a blob where every neighbour is a tree — is exactly what recording `groundType` on the tile already solved.
  - **`dressingFor` ended in `default:` on a closed union**, so adding a sheet row would bake a silently idle-dressed state instead of failing the build. That is precisely how round 5's bug happened. Replaced with an explicit `case 'idle'` plus a `never` fallthrough, and **negative-tested**: adding an undressed state to `TreeState` now fails typecheck with `Argument of type '"unhandled_test_state"' is not assignable to parameter of type 'never'`.
  - **`canopyHalfWidthPx` understated a conifer's real half-width** by measuring `clump.r * NEEDLE_TIP_REACH` from the clump centre and ignoring how far its lobes are thrown outward. It is the one function claiming to know how wide the tree is, and the felling lift is computed from it. Now walks the lobes.
  - **The geometry header claimed bounds it does not enforce** ("nothing here may reach further than 3.7 tiles up or 1.4 sideways" — above a constant of 3.8, and neither constant actually bounds the draw). Rewritten to say what is true: they are the budget the species parameters are picked against, and `assertNothingClipped` is what enforces the envelope.
  - **A comment claimed the splinter clamp used its half-diagonal** while the code used half its length. Rather than fix the comment, fixed the code — `Math.hypot(length, thickness) / 2` — since the comment described the correct thing.
  - Plus: `readManifest` in `townSheets.ts`, a pre-existing export with zero consumers, removed; the harness's third hand-copy of `MAP_SIZE = 280` replaced with `OVERWORLD_MAP_SIZE_TILES`, which exists to be the single definition; and the harness canvas now sizes for the in-game silhouette strip, which at `--scale=1` was wider than the grid and ran off the right edge.
  - **Also corrected:** round 6's journal entry described the collapse dust as scaling "size and alpha by progress". It does not — size is progress-independent and alpha fades out. The shipped mitigation is the `fellProgress > 0` gate, and the *outcome* is verified (`felling` f0 byte-identical to `damaged` f0 on all twelve keys, while f5 differs by thousands of pixels), but the mechanism was described wrongly.
  - Round 9 independently re-measured everything else clean: art drift none, determinism holds, max border alpha 9/255 across 325 frames, 40/40 behaviour assertions, and all §2 non-goals.
- Review round 10: **2 findings, both genuine, both fixed.**
  1. **The round-9 ground fix was inert — my "fix" changed nothing.** Passing `groundType` to `drawTerrainTile` has no effect: its `grass` and `road` cases both call the same `drawGroundTile`, which resolves the material through `groundMaterialUnder` → `groundMaterialAt`, and *that* returned a hard-coded `'grass'` for `TREE` and short-circuited **before** the recorded `groundType` was ever consulted. The reviewer measured it: identical mean RGB either way, and a real road-standing tree still popped 20 channel-levels when felled. **This is the fourth round to catch a claim of mine that was untrue, and the root cause was new: I verified the fix was *present* (grepped for it) but never verified it *worked*.** Grepping proves an edit landed; only measuring proves it did anything.
     Fixed where §5.9 said to all along — in `groundMaterials.ts`, by dropping `case TREE:` from `groundMaterialForTileType`, which is safe now (and was not when that entry was written) because `paintForests` uses `setStanding`, so every tree records its surface and `groundMaterialUnder` consults it before reaching inference. The renderer special-case is reverted. **Measured, not assumed:** a tree standing on road now draws dirt beneath it and reads `rgb(99,78,54)` both before and after felling — max channel difference **0**.
  2. **Death by burning-tree contact reported "unknown".** This change is the first producer anywhere of the `{ kind: 'environmental' }` damage source — `Player.ts:82` had zero call sites before it — and `DeathCauseSystem` had no branch for it, so it fell through to `'unknown'`. §3.9 checked that the `burn` *status* is attributed and stopped there; the contact damage is what actually kills, well before the eight-second DoT gets there. Added a `burningTree` cause with its own explanation lines, and verified end to end that a player killed that way resolves to `'burningTree'` with text attached. Handling the last unhandled variant also made the trailing `kind === 'mob'` test provably always-true, so it is now narrowed by elimination.
- **Review round 11: `NO SUBSTANTIVE FEEDBACK`.** The §7 loop is closed. Round 11 re-measured everything rather than reading it: gates all exit 0; the bake is drift-free and deterministic; **8 road-standing trees across 15 generated overworlds render dirt beneath them and show max channel difference 0 before and after felling**, while 40,244 grass-standing trees are likewise unchanged and deep-blob trees still draw grass — so round 10's fix is real this time, and the common case is not broken. A real `HumanPlayer` killed by tree contact resolves to `'burningTree'`. 29/29 behaviour assertions. `destroyInRadius` 29/29 with the ring lighting 44 trees outside the blast and none inside it. A tree 3 rows below the viewport is still drawn and 4 rows is not, confirming the canopy-cull payoff of `tileTypeId`. The minimap pixel walks forest → charcoal → grass.
- Correction: this journal said the max border alpha across the sheets was 9/255; round 11 measured **3/255**. Both are antialiasing well under the gate's tolerance of 24, but the figure was wrong.
- **Final state: 11 review rounds, 45 findings — 44 fixed, 1 rejected with a recorded reason.** Four of those rounds caught this journal claiming something untrue (a gate that could not fail, an edit that never applied, a measurement taken on the wrong sample, and a fix that was present but inert). The durable lesson, recorded in project memory: *grepping proves an edit landed; only measuring proves it did anything* — and any gate that cannot fail is not a gate.
- Left undone / next: Phase 9's human playtest, and the §5.8 tuning pass that needs play rather than a screenshot. (Phases 4-8 have not been reviewed by anyone yet), then Phase 9's human playtest. Every §5.8 tuning value is still at its plan default — nothing has been re-tuned, because tuning needs play, not a screenshot, and the headless check confirms the defaults behave as specified.

---

## 10. Risks

| Risk | Mitigation |
|---|---|
| Taller trees widen `_modestDecorationExtents` and slow every decoration row scan | Explicit perf checkpoint in Phase 3 (§5.3); fallback is to push trees into `_oversizedDecorations` |
| A Y-sorted tile registered in only one of the two registries renders as **bare floor**, and typecheck/lint both pass | `TREE` is already in both — do not remove it from either. Re-check `TileRenderer.ts:71-94` and `GameMap.ts:166-188` if trees vanish |
| Twelve trees look like twelve games | One palette, one canopy engine, one light direction (§5.4); Phase 7 judges the *forest*, not the tree |
| Non-determinism in the generator makes diffs meaningless | Fixed literal seeds; Phase 2 runs the generator twice and requires a clean `git status` |
| A new PNG that no manifest names is silently dropped from the shipped build | Phase 8 verifies against `scripts/shipped-assets.js` |
| `drawDecorationTile` has no access to systems, so burn animation can't read `TreeSystem` | Stage + frame cursor live on the tile (§5.9), mirroring how `TORCH`/`BRAZIER` read `damageStage` |
| Burning-tree scan becomes O(forest) per frame | Iterate the burning-state `Map` only; never scan the grid |
| A felled tree reveals the wrong ground material | Phase 4 verifies; fix in `groundMaterials.ts`, not the renderer |
